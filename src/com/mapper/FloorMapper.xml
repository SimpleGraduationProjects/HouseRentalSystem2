<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.dao.FloorDao">
    <resultMap id="BaseResultMap" type="com.model.Floor">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="info" property="info" jdbcType="VARCHAR"/>
        <result column="del" property="del" jdbcType="VARCHAR"/>
        <result column="addressLat" property="addressLat" jdbcType="VARCHAR"/>
        <result column="addressLon" property="addressLon" jdbcType="VARCHAR"/>
    </resultMap>
    <!-- 插入 -->
    <insert id="saveEntity" parameterType="com.model.Floor">
    insert into floor (id, name, info, del,addressLat,addressLon)
    values (#{id,jdbcType=INTEGER}, #{name,jdbcType=VARCHAR},
    #{info,jdbcType=VARCHAR}, #{del,jdbcType=VARCHAR},
     #{addressLat,jdbcType=VARCHAR},  #{addressLon,jdbcType=VARCHAR})
  </insert>

    <!-- 查询列表 -->
    <select id="searchEntity" resultMap="BaseResultMap" parameterType="map">
        SELECT * FROM floor where 1=1
        <if test="keyword != null and keyword != ''">
            and name like CONCAT('%',#{keyword},'%')
        </if>
        ORDER BY floor.id desc

        <if test="(start != null and start != '') or start==0 ">
            <if test=" limit != null and limit != ''">
                limit #{start},#{limit}
            </if>
        </if>

    </select>


    <!-- 统计 获取 总数    用于分页-->
    <select id="count" resultType="int" useCache="false" parameterType="map">
        SELECT count(1) FROM floor where 1=1
        <if test="keyword != null and keyword != ''">
            and name like CONCAT('%',#{keyword},'%')
        </if>


    </select>

    <!-- 通过主键编号获得map对象 -->
    <select id="getEntityById" parameterType="int" resultMap="BaseResultMap">
  	SELECT * FROM floor WHERE id = #{id}
  </select>

    <!-- 通过主键删除 -->
    <delete id="deleteEntity" parameterType="int">
  	DELETE FROM floor WHERE id = #{id}
  </delete>

    <!-- 更新数据库 -->
    <update id="updateEntity" parameterType="com.model.Floor">
    update floor
      set
      name = #{name,jdbcType=VARCHAR},
      info = #{info,jdbcType=VARCHAR},
      del = #{del,jdbcType=VARCHAR},
      addressLat = #{addressLat,jdbcType=VARCHAR},
       addressLon= #{addressLon,jdbcType=VARCHAR}
      where id = #{id}
  </update>

</mapper>