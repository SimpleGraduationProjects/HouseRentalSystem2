<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dao.NewsDao">
    <resultMap id="BaseResultMap" type="com.model.News">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Wed Apr 11 20:22:07 CST .
        -->
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="title" jdbcType="VARCHAR" property="title"/>
        <result column="user_id" jdbcType="INTEGER" property="userId"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
    </resultMap>
    <resultMap extends="BaseResultMap" id="ResultMapWithBLOBs" type="com.model.News">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Wed Apr 11 20:22:07 CST .
        -->
        <result column="content" jdbcType="LONGVARCHAR" property="content"/>
    </resultMap>

    <!-- 插入 -->
    <insert id="saveEntity" parameterType="com.model.News">
  	  INSERT INTO news (title, content, user_id, create_time)
    values (#{title}, #{content}, 1, #{createTime})
  </insert>

    <!-- 查询列表 -->
    <select id="searchEntity" resultMap="BaseResultMap" parameterType="map">
        SELECT news.*,`user`.name as userName FROM news
        left join `user` on `user`.id=news.user_id
        where 1=1

        <if test="keyword != null and keyword != ''">
            and title like CONCAT('%',#{keyword},'%')
        </if>
        ORDER BY news.id desc
        <if test="(start != null and start != '') or start==0 ">
            <if test=" limit != null and limit != ''">
                limit #{start},#{limit}
            </if>
        </if>

    </select>

    <!-- 统计 获取 总数    用于分页-->
    <select id="count" resultType="int" useCache="false" parameterType="map">
        select count(1) from news
        where 1=1

        <if test="keyword != null and keyword != ''">
            and title like CONCAT('%',#{keyword},'%')
        </if>
    </select>


    <!-- 通过主键编号获得map对象 -->
    <select id="getEntityById" parameterType="int" resultMap="BaseResultMap">
  	SELECT * FROM news WHERE id = #{id}
  </select>

    <!-- 通过主键删除 -->
    <delete id="deleteEntity" parameterType="int">
  	DELETE FROM news WHERE id = #{id}
  </delete>

    <!-- 更新数据库 -->
    <update id="updateEntity" parameterType="com.model.News">
  	UPDATE news SET title = #{title}, 
      content = #{content} WHERE id = #{id}
  </update>

</mapper>