<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.dao.ComplainDao">
    <resultMap id="BaseResultMap" type="com.model.Complain">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
       <result column="customer_id" property="customer_id" jdbcType="VARCHAR"/>
        <result column="info" property="info" jdbcType="VARCHAR"/>
        <result column="title" property="title" jdbcType="VARCHAR"/>
        <result column="complainstatus" property="complainstatus" jdbcType="VARCHAR"/>
    </resultMap>
    <!-- 插入 -->
    <insert id="saveEntity" parameterType="com.model.Complain">
     insert into complain (id, create_time,
       customer_id,
      info, title, complainstatus)
    values (#{id,jdbcType=INTEGER},  #{createTime,jdbcType=TIMESTAMP},
      #{customer_id,jdbcType=VARCHAR},
      #{info,jdbcType=VARCHAR}, #{title,jdbcType=VARCHAR}, #{complainstatus,jdbcType=VARCHAR})
  </insert>

    <!-- 查询列表 -->
    <select id="searchEntity" resultMap="BaseResultMap" parameterType="map">
        SELECT complain.*,  customer.name AS customerName
        FROM complain


        LEFT JOIN customer on customer.id = complain.customer_id
        where 1=1
        <if test="keyword != null and keyword != ''">
            and (
            customer.name like CONCAT('%',#{keyword},'%') or

            complain.info like CONCAT('%',#{keyword},'%')
            )
        </if>

        <if test="customer_id != null and customer_id != ''">
            and customer_id =#{customer_id}
        </if>
        ORDER BY complain.id desc

        <if test="(start != null and start != '') or start==0 ">
            <if test=" limit != null and limit != ''">
                limit #{start},#{limit}
            </if>
        </if>

    </select>


    <!-- 统计 获取 总数    用于分页-->
    <select id="count" resultType="int" useCache="false" parameterType="map">
        SELECT count(1)
        FROM complain

        LEFT JOIN customer on customer.id = complain.customer_id
        where 1=1
        <if test="keyword != null and keyword != ''">
            and (
            customer.name like CONCAT('%',#{keyword},'%') or

            complain.info like CONCAT('%',#{keyword},'%')
            )
        </if>

        <if test="customer_id != null and customer_id != ''">
            and customer_id =#{customer_id}
        </if>

    </select>

    <!-- 通过主键编号获得map对象 -->
    <select id="getEntityById" parameterType="int" resultMap="BaseResultMap">
  	    SELECT complain.*,     customer.name AS customerName
    FROM complain

   LEFT JOIN customer on  customer.id = complain.customer_id
   WHERE complain.id = #{id}
  </select>

    <!-- 通过主键删除 -->
    <delete id="deleteEntity" parameterType="int">
  	DELETE FROM complain WHERE id = #{id}
  </delete>

    <!-- 更新数据库 -->
    <update id="updateEntity" parameterType="com.model.Complain">
    update complain set

      create_time = #{createTime,jdbcType=TIMESTAMP},

      customer_id = #{customer_id,jdbcType=VARCHAR},
      info = #{info,jdbcType=VARCHAR},
      title = #{title,jdbcType=VARCHAR},
      complainstatus = #{complainstatus,jdbcType=VARCHAR}
      where id = #{id}
  </update>

</mapper>