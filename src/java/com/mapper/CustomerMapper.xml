<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.dao.CustomerDao">
    <resultMap id="BaseResultMap" type="com.model.Customer">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="sex" property="sex" jdbcType="VARCHAR"/>
        <result column="age" property="age" jdbcType="VARCHAR"/>
        <result column="tel" property="tel" jdbcType="VARCHAR"/>
        <result column="info" property="info" jdbcType="VARCHAR"/>
        <result column="loginname" property="loginname" jdbcType="VARCHAR"/>
        <result column="loginpassword" property="loginpassword" jdbcType="VARCHAR"/>
    </resultMap>
    <!-- 插入 -->
    <insert id="saveEntity" parameterType="com.model.Customer">
    insert into customer (id, name, sex,
      age, tel, info,
      loginname,
      loginpassword)
    values (#{id,jdbcType=INTEGER}, #{name,jdbcType=VARCHAR}, #{sex,jdbcType=VARCHAR},
      #{age,jdbcType=VARCHAR}, #{tel,jdbcType=VARCHAR}, #{info,jdbcType=VARCHAR},
      #{loginname,jdbcType=VARCHAR},
      #{loginpassword,jdbcType=VARCHAR})
  </insert>

    <!-- 查询列表 -->
    <select id="searchEntity" resultMap="BaseResultMap" parameterType="map">
        SELECT * FROM customer
        where 1=1
        <if test="keyword != null and keyword != ''">
            and
            (
            `loginname` like CONCAT('%',#{keyword},'%') OR
            `name` like CONCAT('%',#{keyword},'%') OR
            tel like CONCAT('%',#{keyword},'%') OR
            info like CONCAT('%',#{keyword},'%')
            )
        </if>

        ORDER BY customer.id desc

        <if test="(start != null and start != '') or start==0 ">
            <if test=" limit != null and limit != ''">
                limit #{start},#{limit}
            </if>
        </if>

    </select>


    <!-- 统计 获取 总数    用于分页-->
    <select id="count" resultType="int" useCache="false" parameterType="map">
        SELECT count(1)
        FROM customer
        where 1=1
        <if test="keyword != null and keyword != ''">
            and
            (
            loginname like CONCAT('%',#{keyword},'%') OR
            `name` like CONCAT('%',#{keyword},'%') OR
            tel like CONCAT('%',#{keyword},'%') OR
            info like CONCAT('%',#{keyword},'%')
            )
        </if>

    </select>

    <!-- 通过主键编号获得map对象 -->
    <select id="getEntityById" parameterType="int" resultMap="BaseResultMap">
  	SELECT * FROM customer WHERE id = #{id}
  </select>

    <!-- 通过主键删除 -->
    <delete id="deleteEntity" parameterType="int">
  	DELETE FROM customer WHERE id = #{id}
  </delete>

    <!-- 更新数据库 -->
    <update id="updateEntity" parameterType="com.model.Customer">
    update customer set
      name = #{name,jdbcType=VARCHAR},
      sex = #{sex,jdbcType=VARCHAR},
      age = #{age,jdbcType=VARCHAR},
      tel = #{tel,jdbcType=VARCHAR},
      info = #{info,jdbcType=VARCHAR},
      loginname = #{loginname,jdbcType=VARCHAR},
      loginpassword = #{loginpassword,jdbcType=VARCHAR}
      where id = #{id}
  </update>


    <!-- 判断登录 -->
    <select id="customerLogin" parameterType="map" resultMap="BaseResultMap">
  	SELECT * FROM customer WHERE loginname = #{loginname} AND loginpassword = #{loginpassword}
  </select>


</mapper>